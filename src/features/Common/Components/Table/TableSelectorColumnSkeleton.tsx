import { LoadingSkeleton } from '@components/Loading';

const TableSelectorColumnSkeleton = () => {
  return <LoadingSkeleton className="h-5 w-5 rounded-md" />;
};

export default TableSelectorColumnSkeleton;
