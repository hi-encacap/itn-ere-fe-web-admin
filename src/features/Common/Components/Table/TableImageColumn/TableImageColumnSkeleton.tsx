import { LoadingSkeleton } from "@components/Loading";

const TableImageColumnSkeleton = () => {
  return <LoadingSkeleton className="mx-auto h-12 w-12 rounded-full" />;
};

export default TableImageColumnSkeleton;
