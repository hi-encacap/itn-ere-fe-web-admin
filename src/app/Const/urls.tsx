const AUTHENTICATION_PATH = {
  LOGIN_PATH: '/auth/login',
};

const MY_PATH = {
  HOME_PATH: '/my',
  DOCUMENT_PATH: '/my/documents',
  TEMPLATE_PATH: '/my/templates',
};

const ADMIN_PATH = {
  HOME_PATH: '/admin',
};

const SYSTEM_PATH = {
  HOME_PATH: '/system',
};

export { AUTHENTICATION_PATH, MY_PATH, ADMIN_PATH, SYSTEM_PATH };
